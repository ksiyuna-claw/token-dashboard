# 变更说明 2026-08-27 · token看板：定时任务总览"用途"列空值补全（"—"问题）

> 匡书记 11:58 报修：系统 crontab 板块有一个"—"没显示清楚，要求总体检查同类问题。
> 执行虾：基围虾（CTO）｜性质：代码小修（数据标注层补全，2 文件）

## 一、排查结论（证据链）

- 直接病灶：`gateway_log_rotate.sh` 这条 crontab 在 `_get_crontab()` 的 if/elif 关键词匹配表中无对应分支 → purpose 空串 → 前端第825行 `c.purpose || '—'` 兜底渲染成破折号
- 总体检查扩大到三组数据源（curl /cron-json 实测），共发现三类空值：
  1. **系统 Crontab 表**：1 条空用途（日志轮转，上述根因）
  2. **OpenClaw 定时任务表**：13 个 job 中 12 个空用途——CRON_PURPOSE 字典键名是旧一代 job 名（如「虾厂巡检(凌晨)」），现役 job 已改名（如「凌晨巡检(06:00)」），改名后字典无人同步 → `.get(name,'')` 大面积 miss，只有「Git自动备份」一条碰巧同名匹配上
  3. **系统服务表**：`ai.hermes.watchdog` 不在 LAUNCH_PURPOSE 字典中 → 空
- 成因共性：purpose 标注是静态字典/分支表，job 改名、新服务上线时没有同步维护的机制，属文档腐烂同款病根

## 二、改动清单

| 文件 | 改动 | 验证 |
|------|------|------|
| token_dashboard_server.py | ① _get_crontab() 补 `gateway_log_rotate` 分支 ② CRON_PURPOSE 字典按 sqlite 实际 job 名全量重写（13 项全覆盖，含4个停用 job 明确标"历史停用"，颜姐快照标注停用原因指向 issue-0157）③ LAUNCH_PURPOSE 补 ai.hermes.watchdog 条目 | py_compile 过；重启后 /cron-json 实测三组零空值 |
| templates/token_dashboard.html | 三处兜底 `'—'` 改为 `'未标注'`（哑符号改有含义文案，未来再出现漏标可自解释） | grep 确认 3 处替换；无其他 '—' 残留渲染点 |

## 三、验证记录（修复后实测）
- 服务 launchctl 重启，首页 HTTP 200
- /cron-json 实测：crontab 14 条全部有 purpose（含新补的日志轮转）；openclawCron 13/13 有用途；launchAgents 15 个全部有 purpose
- python3 -m py_compile 通过

## 四、风险与回滚
- 均为显示层数据标注，不动统计/投递逻辑；条目描述文字如与实际不符可直接改字典重载
- 回滚：cp tmp/*bak2.20260827 还原两文件 + kickstart 重启

## 五、遗留说明
- CRON_PURPOSE/LAUNCH_PURPOSE 后续仍靠手工同步（job 改名时要记得改字典），彻底解法需在 workflow.sh 或 Gateway 层挂钩自动维护——本期不做，避免扩大改造面
