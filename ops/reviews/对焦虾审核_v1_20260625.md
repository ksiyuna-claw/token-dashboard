# 对焦虾审核 — Token 看板过滤 dashscope 展示 v1

**审核时间**：2026-06-25 13:47
**审核类型**：治理类审核（简化流程，第1轮/最长3轮）
**审核人**：对焦虾（deepseek-v4-pro）
**结论**：✅ **通过**（通过5项 / 需改进0项红色 / 1项🟡可选建议）

---

## 审查要点逐项核对

### ✅ 要点1：改动范围 + 只过滤展示 + 无副作用
- `token_dashboard_server.py`
  - `PROVIDER_QUOTA_API`：`'dashscope.aliyuncs.com'` 项已移除并替换为解释性注释（line 91-93），注释清晰说明原因+红线提醒。
  - `PROVIDER_LABELS`：dashscope 标签已移除，仅剩 zhipu/zai/deepseek/minimax 四项。
  - 逻辑验证：dashscope baseUrl 不再匹配任何 host → `_get_providers()` 中 `quota_api=None, quota_type=None` → `if not quota_api and quota_type != 'no_quota'` 判为真 → `continue` → dashscope 被跳过。逻辑正确。
- `token_dashboard.html`
  - `else if (p.quotaType === 'no_quota')` 分支已移除，渲染链现为 zhipu_quota → deepseek_balance → minimax_quota。
  - **关键正确性**：`renderNoQuota` 函数**正确保留**（未误删）——它仍被各 quota 分支的错误降级路径复用（如 `代理错误`/`无余额信息`/`获取失败`）。若误删该函数会导致 ReferenceError。改动者准确区分了「删分支」与「保函数」，无副作用。

### ✅ 要点2（🔴红线）：openclaw.json dashscope provider 完整未动
grep 核对 openclaw.json：
- provider block（line 840-842）：`baseUrl=https://dashscope.aliyuncs.com/compatible-mode/v1` ✅、`apiKey=sk-e6aed28...`（sk-e6a 开头）✅、`models` 列表完整（qwen-plus/qwen-turbo/...）✅
- model alias（line 45-58）5 个别名全在 ✅
- 红线完整保住，未被误改。

### ✅ 要点3：代码正确性
- `python3 -m py_compile token_dashboard_server.py` → **PY_COMPILE_OK**
- HTML 括号配对：`{}`=374:374、`()`=557:557、`[]`=70:70，全部平衡。
- grep `dashscope|no_quota|百炼` 于 HTML → 0 匹配，残留引用已彻底清除。

### ✅ 要点4：逻辑正确（端点实测）
`curl http://127.0.0.1:18888/providers` 实测返回 **deepseek / zai / zhipu / minimax** 四个 LLM provider，**无 dashscope**。与预期一致，看板不再渲染「阿里云百炼」卡片。

### ✅ 要点5：服务稳定运行
进程核对：PID 21244，13:45 启动，运行中（重启后稳定）。

---

## 🟡 建议（可选，不阻断）
- `_get_providers()` 中 `quota_type != 'no_quota'` 现已成恒真判断（移除 dashscope 后再无 provider 会被赋 `no_quota` 类型）。逻辑无害但略冗余，后续若确认不再支持 no_quota 类型 provider，可简化为 `if not quota_api: continue`。属代码整洁度优化，**不影响功能正确性，本轮不要求修改**。

---

## 总结
治理类简化流程，5 项审查要点全部满足，红线（openclaw.json dashscope provider）完整未动。本次改动语义清晰、范围克制、逻辑正确、无副作用。**审核通过。**
