# 变更说明 2026-08-29 · token看板：定时任务总览新增「模型」列（实际运行模型自动提取）

> 匡书记 00:54 需求：看板显示各 cron 使用的模型，要求自动提取系统信息、根据实际情况变更（拒绝手工静态维护）。
> 00:58 拍板"加"，00:59 补充"加完走审核流转"。
> 执行虾：基围虾（CTO）｜性质：显示层增强（2 文件）

## 一、方案与证据链

- **数据源选择**：session 元数据的 model 字段是配置值，不可信（server 第306行既有注释）。实际模型必须从 transcript 提取——与看板既有统计口径（`_recent_model_dist`）一致。
- **cron run 的存储结构（sqlite 实证）**：`agents/{agentId}/agent/openclaw-agent.sqlite` 的 `sessions` 表中，cron job 有父 session（`agent:{aid}:cron:{jobId}`）+ 每次运行独立 run session（`agent:{aid}:cron:{jobId}:run:{runId}`）。当前版本 cron 全部在 run session 里跑（父 session 只有早期 glm-5.2 历史残留 5 条）。
- **提取路径验证**：晚间巡检 job 最新 run session（updated_at=1787922900318，昨晚21:15）的 assistant 消息 model=glm-5.3-flash，与 payload 配置一致 → 路径正确。

## 二、改动清单

| 文件 | 改动 | 验证 |
|------|------|------|
| token_dashboard_server.py | ① 新函数 `_cron_job_recent_model(job_id, agent_id)`：LIKE 匹配最新 run session（无则 fallback 父 session）→ 复用 `_read_session_transcript` 取最新 assistant 实际模型；带独立 TTL 缓存（复用 `_dist_cache_lock`，key 前缀 `cronmodel\|` 区分）② `_get_openclaw_cron()` 每个 job 新增 `configModel`（payload 配置值）与 `lastModel`（实际运行值）两字段 | `python3 -m py_compile` 过；重启后 `/cron-json` 实测 14 job 两字段全返回 |
| templates/token_dashboard.html | ① cron 表头加「模型」列（第8列）② 行渲染：优先显示实际模型（hover 全名+"最近一次实际运行"），无实际值显示配置值+"·配"标注，均无显示"默认"（继承全局）③ 两处 `colspan` 7→8 | grep 确认表头/空行 colspan 同步改；首页 HTTP 200 |

## 三、验证记录（修复后实测）

- `python3 -m py_compile` 通过
- 服务重启：`launchctl kickstart -k gui/$UID/com.openclaw.token-dashboard`（非 Gateway，符合操作边界）
- 首页 HTTP 200；`/cron-json` 14 job 实测（节选）：
  - 晚间巡检 → 实际 glm-5.3-flash ✓ 配置 flash ✓
  - 颜姐每日卡片 → 实际 k3 ✓ 配置 kimi/k3 ✓
  - 聚萤财税提醒 → 配置 flash、实际 —（9-1 首跑后自动出现，符合预期）
  - 从未跑过的停用 job → 实际 — ✓
- 系统负担：每次查询 1 次 sqlite 索引查询 + 1 次 transcript 读（LIMIT 30），10s TTL 缓存兜底；/cron-json 按需调用。14 job 量级毫秒级，无感知负担。

## 四、自动性说明（呼应"根据实际情况变更"要求）

- 实际模型列天然动态：每次从最新 run session 的 transcript 提取，job 换模型、全局默认变化、fallback 触发，下次运行后看板自动跟随，**零人工维护**，不存在 8-27 purpose 字典那种改名腐烂问题。
- 配置值列直接来自 Gateway CLI 实时输出，同样自动。

## 五、风险与回滚

- 纯显示层新增字段 + 新列，不动统计/投递逻辑；异常路径全部 try/except 兜底返回 None（显示"—/默认"），不影响其他列。
- 回滚：`cp tmp/token_dashboard_server.py.bak.20260829` 与 `tmp/token_dashboard.html.bak.20260829` 还原两文件 → kickstart 重启，一分钟内可逆。
