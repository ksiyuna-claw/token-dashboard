# 变更说明 2026-09-07 · Token看板：定时任务总览三项迭代（用途补全/停用显示方案B/前端折叠）

> 匡书记 11:52 拍板三点：①用途标注补全25条空缺 ②停用任务显示选方案B（灰条可追溯） ③新增前端折叠优化（看板太长）。
> 否决项：不加硅基额度监测（匡书记明确否决，未做）。
> 执行虾：基围虾（CTO）｜性质：显示层迭代（2 文件：server.py + 模板）

## 一、改动清单

### ① 用途标注补全（25条空缺 → 0）
| 文件 | 改动 |
|------|------|
| token_dashboard_server.py | 新增 `_cron_purpose_fill(name, enabled)` 动态补全函数：静态字典 `CRON_PURPOSE` miss 时按 job 名前缀规则生成（根治 08-27 发现的「改名后字典无人同步」腐烂病根，规则型 job 零维护）；`_get_openclaw_cron._fetch_one` 接入 `CRON_PURPOSE.get(name,'') or _cron_purpose_fill(...)` |

标注内容（与方案逐条对齐）：
- `skill-collection-review-{agent}` ×12 → `系统自动 · Skill Workshop每周收藏集检查（轻量秒级，几乎零token）`
- `heartbeat-{agent}` ×11（禁用态）→ `心跳monitor job（{虾名}）· 已禁用`（动态读 enabled，启用/禁用自动跟随）
- `heartbeat-jiweixia`（启用态）→ `✅ iso模式（09-07启用）：隔离会话+轻上下文，token预计降90%+`
- `secrets注入探针(issue-0226)` → `安全 · secrets注入探针（issue-0226 历史验证用，已停用）`
- 顺手收尾：crontab 活跃区唯一残留「未标注」行（du_snapshot，每天07:30）补用途分支——与 08-27 迭代「三组零空值」目标对齐

### ② 停用任务显示方案B（灰条可追溯）
| 文件 | 改动 |
|------|------|
| token_dashboard_server.py | `_get_crontab()` 新增注释行解析：`[停用YYYYMMDD...]/[暂停YYYYMMDD...]` 标记 + 原 cron 行 → `enabled=False` 条目（保留 schedule/scheduleHuman/purpose/command + 新增 disabledAction/disabledDate/disabledReason）；purpose 匹配链抽取为 `_crontab_purpose()` 供活跃行/停用行共用 |

- 9 条停用 crontab 入列（2条停用08-22余额不足 / 6条暂停08-28项目暂停 / 1条停用09-07死信补发撤除）
- 修正移植中发现的死匹配：econ_result_analyzer 的 `'13:' in line` 永假（cron 表达式 `15 13 * * *` 无"13:"字样）→ 改为解析调度小时字段，13:15 条正确标「美盘 8:30 数据」
- 前端三层停用态样式统一：OpenClaw disabled job（17条，含5条[手动停用]历史任务）与 launchd stopped（5条）均收进各层「已停用(N)」折叠组，灰底灰字+任务名删除线

### ③ 前端折叠优化（匡书记新增：内容太多太长）
| 文件 | 改动 |
|------|------|
| templates/token_dashboard.html | ① 三层数据表改为 `<details class="cron-fold">` 可折叠分组 + 心跳层同样改造（第四层）② summary 右侧显示计数+异常摘要（如 `22 启用 · 17 停用 · ✅无异常`，异常数红色高亮）③ 每层内「已停用(N)」JS 折叠组（`toggleDis`/`applyDisState`，展开状态跨5分钟轮询保持）④ 副标题同步改为 启用/停用 双口径 ⑤ 新增折叠与停用组 CSS |

- 默认视图：三层全收起，只显示计数+异常摘要；点开看明细；停用条目默认收进「已停用(N)」组
- 现有功能与数据不丢：纯前端展示层改造，列结构/模型列/命令展开等原有交互保留

## 二、验证记录（修复后实测）

- `python3 -m py_compile` 通过；服务 `launchctl kickstart -k`（非Gateway，符合边界）后首页 HTTP 200
- `/cron-json` 数据层实测：openclawCron 39 条 **0 空缺**；crontab 10 活跃（0 未标注）+ **9 停用**（带停用日期/原因，8:30/14:00 精准区分）；launchAgents 15（在役10/停止5）；du_snapshot 用途命中
- 浏览器实测（CDP Chrome 18800 驱动，4张截图存 `docs/验证_0907_*.png`）：
  - 默认态：三层折叠收起，摘要计数正确 ✅
  - 展开态：停用组行数 OpenClaw 17 / Crontab 9 / launchd 5 全部可见，灰条+删除线+🚫停用日期原因渲染正确 ✅
  - 再点收起：停用行归零（折叠交互往返正常）✅
- 说明：OpenClaw 停用条目 12→17 系首轮部分 agent 的 `cron list --all` 查询超时未返回，重启后完整返回（含5条[手动停用]历史任务），非改动引入

## 三、风险与回滚

- 纯显示层改造，不动统计/投递/调度逻辑；`_crontab_purpose` 抽取为等价重构（唯一行为修正是 econ 小时死匹配，属标注精度修复）
- 回滚：`cp ~/.openclaw/workspace-jiweixia/tmp/token_dashboard_server.py.bak.20260907` 与 `token_dashboard.html.bak.20260907` 还原两文件 → kickstart 重启

## 四、范围边界

- 只动 token-dashboard 项目内 2 文件；scripts/cron.html（7月15日遗留独立页，非现役渲染面）未动
- 未加硅基额度监测（匡书记否决）
