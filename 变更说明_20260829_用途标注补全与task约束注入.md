# 变更说明 2026-08-29 · token看板：3处用途标注补全 + 皮皮虾选题补充task注入apply_patch禁令

> 匡书记 01:11 报看板新增"未标注"、看脸选题补充假失败、周日回流状态"？"三问；01:14 调查汇报后 01:21 拍板"同意"执行推荐动作①②。
> 执行虾：基围虾（CTO）｜性质：数据标注层补全 + cron task 单行约束注入（🟢-记录类免审：纯新增/不改既有语义/不涉状态决策，8-27 同性质先例）

## 一、改动清单

| # | 文件/对象 | 改动 | 验证 |
|---|----------|------|------|
| 1 | token_dashboard_server.py CRON_PURPOSE 字典 | 新增 `'颜姐-周日回流（皮皮虾）': '内容 · 颜姐账号周批回流（8-28拍板周批模式，周日07:30）'`（该 job 8-28 新建，8-27 字典无它） | py_compile 过；重启后 /cron-json 实测三组零空值，新条目显示正确 |
| 2 | token_dashboard_server.py _get_crontab() 分支表 | 新增 2 分支：cron_delivery_snapshot → "[虾厂运维] cron投递状态快照（7天滚动日志）"；tg_deadletter_resend → "[虾厂运维] TG死信队列自动重发（每分钟）"（两脚本均 8-27 排查投递问题时新增，分支表未覆盖） | 同上（crontab 组零空值） |
| 3 | cron job「看脸实验室-每周选题补充（皮皮虾）」payload message | 首段后注入单行约束："🔴 写 ai_workspace 公共区文件一律用 exec（shell/python），禁用 apply_patch——沙箱限制在 workspace-pipixia 内，写公共区必报 Path escapes sandbox root 并把整个 run 标成失败（2026-08-28 实证两次假失败）。" 注入幂等（检测"禁用 apply_patch"标记已存在则跳过） | edit 返回码 0；复核约束存在=True（message 3321→3479 字符）；排期未变（40 22 * * 5 Asia/Shanghai）、model 未变（default） |

## 二、背景（调查结论摘要）

- 8-28 晚皮皮虾该 job 两次 run（22:20/22:30，非排期点）均因 apply_patch 写公共区选题池被沙箱拦截（"Path escapes sandbox root"）被标 error，但 transcript 证实交付物完整（4 新选题入池、TG 推送成功、自检落盘）——假失败。历史各周用 exec 写均正常。
- 约束注入后，下次排期（9-4 周五 22:40）起 task 自带禁令，根治假警报。

## 三、验证记录

- `python3 -m py_compile` 通过
- `launchctl kickstart -k gui/$UID/com.openclaw.token-dashboard` 重启后首页正常
- `/cron-json` 实测：openclawCron / crontab / launchAgents 三组 purpose 全部非空（此前 openclaw 空1、crontab 空2）
- job message 复核见上表

## 四、风险与回滚

- 标注补全：纯新增字典/分支条目，不触任何统计逻辑。
- job message 注入：一行约束，不改排期/模型/投递配置；如需回滚可 `openclaw cron edit <id> --message <原文本>`（原文本 3321 字符，无备份落盘——从 sqlite cron_jobs 表可再取）。
- server.py 回滚：`cp tmp/token_dashboard_server.py.bak2.20260829` 还原 + kickstart 重启。
