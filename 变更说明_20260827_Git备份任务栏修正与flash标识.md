# 变更说明 2026-08-27 · token看板：Git备份任务栏修正 + flash模型标识

> 匡书记 10:57 两点反馈：①看板是否支持 5.3-flash 显示 ②Git自动备份在系统任务栏恒显「暂无运行记录」要不要修。
> 执行虾：基围虾（CTO）｜性质：代码小修（2 文件，含 1 处死配置清除 + 1 处显示增强）

## 一、排查结论（证据链）

### 问题① flash 显示
- **结论：统计层面已支持，仅有缩写区分缺口**
- MODEL_COLORS 按 `'glm'` 关键词匹配绿色（token_dashboard.html 第305行），modelAbbr 返回 'GLM'
- 今晨快照已含 `glm-5.3-flash` 调用 1 条（token_snapshot.json 实证，modelDist 正常计入）
- 缺口：压缩模式下 glm-5.3 与 glm-5.3-flash 都显示缩写 'GLM'，灰度期无法从看板分辨谁在跑 flash

### 问题② Git备份「暂无运行记录」
- **结论：任务本身健康，是看板侧的死配置，不是故障**
- sqlite 实证（openclaw.sqlite cron_jobs）：Git自动备份 2026-08-27 06:28:00 运行，status=ok，delivered，56.6s
- 根因：「系统任务栏」（8-20 上线）设计语义 = 只盯机器级 cron/launchd 进程，扫描器第一道规则就排除一切 Gateway 派生进程（`_gateway_derived_pids` 注释与实现实证）
- 而 Git备份是 OpenClaw cron job（agent_id=jiweixia），git_backup.sh 由 Gateway 派生执行 → 名单里那条 `{'name':'Git自动备份','match':['git_backup.sh',...]}` 自上线起**永远匹配不到 = 死配置**，state 文件中它零记录（data/system_tasks_state.json 实证只有其他 5 项）
- 其真实运行状态一直可以在 `/cron` 定时任务总览页看到（/cron-json 返回 13 jobs 含完整 lastRunStatus）

## 二、改动清单

| 文件 | 改动 | 验证 |
|------|------|------|
| token_dashboard_server.py | DEFAULT_SYSTEM_TASKS 移除「Git自动备份」死条目，附注释说明原因与去向（其余 5 项不动） | python3 -m py_compile 过；import 后 `_load_system_task_defs()` 断言不含该名 ✓ |
| templates/token_dashboard.html | modelAbbr() 新增 flash 先判分支：`glm+flash → 'Flash'`，否则维持 'GLM'；带日期注释 | 改动为纯 JS 单分支插入，上下文行号核验（第322-325行）；服务重启后首页 HTTP 200 |

## 三、验证记录
- 改前备份：两文件 cp 至基围虾 workspace tmp/（*bak.20260827），回滚可直接 cp 还原
- 服务重启：`launchctl kickstart -k gui/$UID/com.openclaw.token-dashboard`（非 Gateway，符合操作边界），首页 200，/cron-json 13 jobs 正常
- 交叉验证数据源：/cron-json 的 openclawCron 里 Git备份 lastRunStatus 字段完好（该页不受本次改动影响）

## 四、风险评估
- 移除死条目：UI 上 Git备份从「系统任务栏」消失，避免误导性「暂无运行记录」；其状态查看入口本就在 /cron 总览页（页面顶部有链接），无信息丢失
- flash 缩写：仅影响压缩模式标签文字，不影响任何统计逻辑
- 两处均为低风险显示层改动，无数据迁移、无接口变更

## 五、回滚方案
cp tmp/ 备份还原两文件 → kickstart 重启，一分钟内可逆。
