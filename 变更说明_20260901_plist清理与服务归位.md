# 变更说明 20260901：plist 清理 OPENCLAW_HOME + 服务从灾难快照目录归位

> 执行：基围虾（jiweixia）｜匡书记 19:30 拍板｜对焦虾审核对象：本文件 + plist + 进程实证

## 背景

两个关联问题（同根：中午 `.openclaw` 灾难重命名事件）：

1. **plist 非常规变量**：`com.openclaw.token-dashboard.plist` 里 `OPENCLAW_HOME=/Users/kuangsiyu` —— issue-0106 嵌套影子库的成因配置模式。
2. **进程 cwd 残留快照目录**：pid 5232 于 8/31 00:17 从真实路径启动；9/1 中午 `.openclaw` 被重命名为 `.openclaw.81-disaster-20260901_1321` 后，运行中进程的 cwd 跟随 inode 留在快照目录内，且 `templates/token_dashboard.html` 按请求从 `__file__` 所在目录读 → 进程在从快照目录读模板。

## 关键发现（任务前提修正，需匡书记知悉）

任务前提：「CEO 已 grep 验证项目 *.py 代码不消费 OPENCLAW_HOME，删除安全」。
**实证结果：前提只对了一半。**

- Python 代码确实零消费（全项目全文件类型 grep 零匹配，含 config.json/templates）。✅
- 但服务端 spawn 的 `openclaw` CLI 子进程继承环境变量，共 3 个调用点：
  `token_dashboard_server.py:943`（sessions）、`:1462`（cron list）、`:1681`（config get）。
- **launchd 极简环境（仅 HOME+PATH）下，无 OPENCLAW_HOME 时 CLI 把 state 目录解析到不存在的嵌套路径 `/Users/kuangsiyu/.openclaw/.openclaw/`（issue-0106 影子模式），sessions 返回 0 条。**
- 对照实验：极简环境 + `OPENCLAW_HOME=/Users/kuangsiyu`（旧值）→ 真实路径 293 会话。即旧值在极简环境下是功能必需，不是装饰。
- 直接删除导致看板 sessions 面板归零（46→0 条实测），构成回归。

**修正方案**：改用语义精确的 `OPENCLAW_STATE_DIR=/Users/kuangsiyu/.openclaw` 显式钉住 state 目录（openclaw paths 模块解析第一优先级：`override = env.OPENCLAW_STATE_DIR` 直指目录，无 HOME 覆盖 footgun，不影响默认安装身份判定）。极简环境实测：真实路径、293 会话、零嵌套。✅

## 变更清单

| # | 对象 | 变更 | 工具 |
|---|------|------|------|
| 1 | plist | 删除 `EnvironmentVariables:OPENCLAW_HOME` | PlistBuddy（plist 禁 sed/正则） |
| 2 | plist | 新增 `EnvironmentVariables:OPENCLAW_STATE_DIR=/Users/kuangsiyu/.openclaw` | PlistBuddy |
| 3 | 服务 | bootout → bootstrap 重启，进程归位真实路径 | launchctl 分步 |
| 4 | 备份 | plist 原件备份至 `ops/com.openclaw.token-dashboard.plist.bak-20260901-1935`（sha256 校验一致） | cp |

最终 EnvironmentVariables：`HOME=/Users/kuangsiyu` + `PATH=...` + `OPENCLAW_STATE_DIR=/Users/kuangsiyu/.openclaw`。
stderr/stdout 保持指向可读日志（`~/.openclaw/logs/token-dashboard/`），未动。WorkingDirectory 未动（本就正确）。
未触碰 `.81-disaster` 快照目录（按红线，另案待拍板）。

## 验证矩阵（issue-0107 完整启动链，8/8 通过）

| # | 项 | 结果 |
|---|----|------|
| V1 | 新进程(45391) cwd | ✅ `~/.openclaw/workspace/ai_workspace/projects/token-dashboard` |
| V2 | 进程环境 | ✅ 有 OPENCLAW_STATE_DIR，无 OPENCLAW_HOME |
| V3 | 快照路径依赖 | ✅ lsof 打开 `.81-disaster` 文件数 = 0 |
| V4 | 页面 | ✅ GET / 200、/login 200、health-json ts age 0.1s |
| V5 | sessions 数据（回归项） | ✅ 登录后 /sessions-json 返回 50 条真实 agent 数据，error 无 |
| V6 | 快照持久化 | ✅ token_snapshot.json 恢复 45 条、updatedAt 实时 |
| V7 | 日志 | ✅ /tmp/token_dashboard.log 新启动日志、WS 已连 Gateway 并 subscribe |
| V8 | launchctl | ✅ `45391 0 com.openclaw.token-dashboard` 无异常 |

附加抽查：/cron-json 正常返回实时 cron 数据（第 2 个 CLI 调用点）。

## 遗留风险与建议

- Gateway 自身环境仍带 `OPENCLAW_HOME=/Users/kuangsiyu/.openclaw`（本次实测发现，Gateway 子进程 shell 可见）。这是 issue-0106 影子目录模式的存活源头之一，**本次未动**（超出任务范围），建议另案排查 Gateway plist/env 的该变量。
- 极简环境无 OPENCLAW_HOME 时 CLI 默认解析到嵌套路径的行为疑似 openclaw CLI bug（HOME 已设仍嵌套），值得向官方反馈。
